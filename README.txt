VIOLETBOT static site - updated build

How to use:
1. Extract the ZIP.
2. Upload the whole folder to Netlify / GitHub Pages / Cloudflare Pages.
3. Replace files inside the downloads folder with your real ZIP packages later.
4. Replace any placeholder images inside the images folder if you want.

Notes:
- This build includes only the classes requested from the screenshot.
- Skill Setup is now plain editable text.
- Two images (Mystic Succession and Shai) were not supplied as separate class images, so placeholder images were generated for them.
